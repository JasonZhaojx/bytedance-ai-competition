## 系统架构图

原来的单张大图会被 Markdown 预览器缩得很小，所以这里拆成 4 张图：一张总览图，三张局部调用图。
### 1. 系统分层总览

```mermaid
%%{init: {"theme": "base", "themeVariables": {"fontSize": "18px", "fontFamily": "Microsoft YaHei, Arial", "primaryColor": "#eef2ff", "primaryBorderColor": "#6366f1", "lineColor": "#475569"}}}%%
flowchart TD
    A["前端交互层<br/>任务创建、竞品选择、报告预览、问卷中心、知识库问答"]
    B["后端服务层<br/>接口路由、任务状态、文件读写、子进程管理"]
    C["智能体编排层<br/>竞品发现、单品分析、报告生成、质检反馈、问卷调研、知识库构建"]
    D["模型调用层<br/>统一封装大模型请求，按配置切换模型服务"]
    E["数据存储层<br/>报告、问卷、质检记录、知识库文件、本地日志"]
    F["外部服务层<br/>大模型服务、搜索服务、网页抓取服务"]

    A -->|"调用后端接口"| B
    B -->|"启动主流程 / 调用功能模块"| C
    C -->|"请求大模型"| D
    D -->|"访问模型服务"| F
    C -->|"搜索与网页抓取"| F
    C -->|"写入中间产物和最终产物"| E
    B -->|"读取报告、问卷、知识库"| E
```

### 2. 主竞品分析任务调用关系

```mermaid
%%{init: {"theme": "base", "themeVariables": {"fontSize": "18px", "fontFamily": "Microsoft YaHei, Arial", "primaryColor": "#f0fdf4", "primaryBorderColor": "#16a34a", "lineColor": "#334155"}}}%%
flowchart TD
    A["用户创建竞品分析任务"] --> B["后端接收任务请求"]
    B --> C["整理任务配置<br/>模型、搜索、质检、轮数等"]
    C --> D["保存侧端输入<br/>我方参数、问卷分析文本"]
    D --> E["启动主业务进程"]

    E --> F["读取产品需求"]
    F --> G["读取我方参数"]
    G --> H["生成对标参数关键词库"]
    H --> I["读取问卷分析报告"]

    I --> J["竞品发现<br/>生成搜索词并搜索候选产品"]
    J --> K["返回候选竞品列表"]
    K --> L["用户选择要分析的竞品"]

    L --> M["并行分析多个单品"]
    H --> M
    M --> N1["单品报告 1"]
    M --> N2["单品报告 2"]
    M --> N3["单品报告 N"]

    N1 --> O["汇总单品报告"]
    N2 --> O
    N3 --> O

    O --> P["构建报告智能体输入<br/>单品报告 + 参数关键词库 + 问卷分析"]
    P --> Q["报告生成智能体"]

    Q --> Q1["证据结构化"]
    Q1 --> Q2["产品经理洞察"]
    Q2 --> Q3["竞品画像和对比表"]
    Q3 --> Q4["优势劣势机会风险分析"]
    Q4 --> Q5["产品策略建议"]
    Q5 --> Q6["撰写结构化报告"]

    Q6 --> R["报告包<br/>正文、结构化数据、证据映射、生成轨迹"]
    R --> S["质检智能体"]
    S --> T{"是否需要修正"}
    T -->|"需要"| U["生成结构化反馈<br/>补证据 / 改分析 / 改写法"]
    U --> Q
    T -->|"通过或达到轮数"| V["输出最终产物"]

    V --> V1["最终横向报告"]
    V --> V2["分析总报告"]
    V --> V3["结构化报告包"]
    V --> V4["证据卡索引"]
    V --> V5["质检记录"]
```

### 3. 问卷中心调用关系

```mermaid
%%{init: {"theme": "base", "themeVariables": {"fontSize": "18px", "fontFamily": "Microsoft YaHei, Arial", "primaryColor": "#fff7ed", "primaryBorderColor": "#f97316", "lineColor": "#334155"}}}%%
flowchart TD
    A["用户进入问卷中心"] --> B["生成问卷"]
    A --> C["模拟填写"]
    A --> D["分析答卷"]

    B --> B1["输入产品/竞品方向"]
    B1 --> B2["可选搜索竞品信息"]
    B2 --> B3["大模型生成问卷题目"]
    B3 --> B4["保存问卷文件"]

    C --> C1["读取问卷文件"]
    C1 --> C2["按目标人群模拟答卷"]
    C2 --> C3["保存答卷文件"]

    D --> D1["读取问卷和答卷"]
    D1 --> D2["代码统计<br/>选择题、量表题、开放题"]
    D2 --> D3{"答卷是否较多"}
    D3 -->|"不多"| D4["直接生成分析提示词"]
    D3 -->|"较多"| D5["每 50 份切块<br/>并行统计和总结"]
    D5 --> D6["逐级合并各批总结"]
    D4 --> D7["大模型生成问卷分析报告"]
    D6 --> D7
    D7 --> D8["保存问卷分析报告"]

    D8 --> E["作为主任务侧端输入"]
    E --> F["进入报告生成上下文"]
    F --> G["用于校准用户画像、场景、价格敏感度、采购顾虑"]
```

### 4. 知识库skill化构建调用关系

```mermaid
%%{init: {"theme": "base", "themeVariables": {"fontSize": "18px", "fontFamily": "Microsoft YaHei, Arial", "primaryColor": "#fdf4ff", "primaryBorderColor": "#a855f7", "lineColor": "#334155"}}}%%
flowchart TD
    A["用户选择一个报告任务"] --> B["后端构建知识库"]
    B --> C["读取同一任务下的最终报告"]
    B --> D["读取同一任务下的分析总报告"]

    C --> E["拼接为知识库来源文章"]
    D --> E
    E --> F["长文切块"]
    F --> G["并行提炼每块知识要点"]
    G --> H["汇总为知识库维护说明"]

    H --> I["大模型规划知识库目录和文件"]
    I --> J["写入知识库入口文件"]
    I --> K["写入对比表、证据索引、策略手册、风险记录等子文件"]

    E --> L["保留原始报告全文"]
    E --> M["保留原始表格摘录"]
    G --> N["保留分块事实和缺口记录"]

    J --> O["更新知识库清单"]
    K --> O
    L --> O
    M --> O
    N --> O

    O --> P["知识库问答"]
    P --> Q["基于报告数据集复用和追溯结论"]
```

### 调用关系摘要

- 前端负责交互展示，后端负责接口、任务状态和文件读写。
- 主业务由后端启动独立进程执行，避免长任务阻塞页面。
- 智能体编排层把竞品发现、单品分析、报告生成和质检反馈串起来。
- 问卷分析和我方参数不是独立报告结尾才接入，而是作为侧端输入进入主报告上下文。
- 知识库构建读取已有报告，把一次分析沉淀成可复用、可问答、可追溯的资料库。
